from django.urls import path

# Additional user-related URL patterns
# Note: Most user endpoints are defined in the main urls.py file
urlpatterns = [
    # Add any additional user-specific URL patterns here if needed
    # Most patterns are already defined in wellness_project/urls.py
]